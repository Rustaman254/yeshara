# Production build/deploy default — NOT a secret, just the deployed
# backend's public URL. .env.local (gitignored, local-machine-only, never
# shipped to a deployment) overrides this for local dev; see .env.local.
NEXT_PUBLIC_API_URL=https://yeshara-backend.onrender.com
